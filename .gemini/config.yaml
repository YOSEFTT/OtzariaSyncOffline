version: 1.0.0
codeReview:
  include:
    - "**/*.py"
    # במקום לתת לו את כל התיקייה, נגדיר לו ספציפית סיומות בתוך התיקייה הזו
    - ".github/workflows/**/*.yml"
    - ".github/workflows/**/*.yaml"
    
    # אופציונלי: תנסה להשאיר את אלו בהערה לרגע כדי לבודד את הבעיה
    - "**/*.txt" 
    - "**/*.md"
    
    # זה מיותר אם כבר הגדרנו את הספציפיים למעלה, אבל לא מזיק
    # - "**/*.yml" 
      
  exclude:
    - "dist/**"
    - ".kiro/**"
    - "build/**"